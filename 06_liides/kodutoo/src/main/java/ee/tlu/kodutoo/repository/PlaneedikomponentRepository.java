package ee.tlu.kodutoo.repository;

import ee.tlu.kodutoo.entity.Planeedikomponent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlaneedikomponentRepository extends JpaRepository<Planeedikomponent, Long> {
}
