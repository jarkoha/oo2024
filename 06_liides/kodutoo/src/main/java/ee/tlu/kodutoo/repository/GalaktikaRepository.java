package ee.tlu.kodutoo.repository;

import ee.tlu.kodutoo.entity.Galaktika;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GalaktikaRepository extends JpaRepository<Galaktika, Long> {
}
